---
title: "Adobe Workfront"
description: "Manage your work in Workfront"
---

# Adobe Workfront

> Manage your work in Workfront

**Version:** 1.0.0 | **Status:** active | **Category:** Workflow Automation, Product Feature | **Deployment:** remote

**Tags:** `workfront`, `projects`, `workfront-planning`, `tasks`, `approvals`
**Surface:** End-user, Developer
**Vendor:** Adobe
**Owner:** grp-wf-llm-gateway-qa
**Repository:** _N/A_
**Updated:** 2026-08-07

---

## About

Search, summarize, create, and update projects, tasks, workspaces, planning records  and approvals directly from any AI harness. Stay on top of deadlines, surface risks, and move work forward faster.

---

## MCP Capabilities

### Tools

| Name | Description |
|------|-------------|
| `insights_find_id_by_name` | Find and resolve a named Workfront object — project, task, issue, user, portfolio, program, team, document — to its GUID for use in a filter condition. |
| `insights_find_workfront_data` | Use this to answer questions like "show me my tasks", "what's due this week", "list open projects", "find tasks assigned to me", "how many issues are overdue", "which projects are behind schedule", "count tasks by status", "show unassigned tasks", "get planned hours for a project", "find issues submitted this month", "what project templates are available". This is the primary read/query tool for all Workfront data retrieval. REQUIRED: call read_docs("mcp-usage") first if you have not already done so this session. Search, list, filter, count, sort, or retrieve Workfront data — projects, tasks, issues, users, portfolios, programs, teams, documents, milestones; returns rows, columns, totalCount. |
| `insights_get_current_user` | Find and retrieve the current user's Workfront identity (name, ID, URL) for personalizing a response. When to call: when the response should address the user by name or include a direct link to a Workfront object that needs the user's workfront URL as a base. Do NOT call this just because the question mentions "me" / "my" / "mine" — use the `$$USER.ID` sentinel directly inside conditions instead. This tool is for rendering a response, not for filtering. Returns: ok with {user_id, user_name, wf_url}. |
| `insights_get_field_paths` | Resolve and retrieve dot-notation field paths for Workfront entities — projects, tasks, issues, users, portfolios, programs, teams — required by find_workfront_data(). Returns the single best root entity and, per field, all candidate paths. |
| `insights_list_entities` | List all Workfront object types available to query — projects, tasks, issues, portfolios, programs, users, teams, documents, templates, milestones, and more. Needs: - read_docs("mcp-usage") — if not called this session. When to call: at the start of any data query to identify entity IDs for use in search_fields(). Always include "user" when the question involves ownership, assignment, "my", "I", "me", a person's name, or team membership. Do NOT call: more than once per session — the entity catalog is stable for the lifetime of the conversation; reuse the prior result. This does NOT mean you can skip it on the first query: always call it at least once before search_fields(). Returns: list of entities, each with id, label, description, synonyms, and contextualHelpLabel. Use the `id` values as input to search_fields(). |
| `insights_read_docs` | Load the Workfront insights playbook or domain documentation — the required first step before querying, searching, listing, filtering, or counting any Workfront data. Call read_docs("mcp-usage") FIRST in any session before using any other insights tool. This loads the master tool-sequencing playbook. Trigger keywords: find, search, list, show, get, retrieve, display, fetch, filter, query, count, how many, which, what, who, report, lookup, aggregate, sort, group, breakdown, summarize, due, assigned, overdue, upcoming, open, closed, completed, active, late, behind schedule, unassigned, in progress, not started, planned hours, actual hours, percent complete, status, condition. Object types: project, task, issue, user, portfolio, program, team, document, milestone, hour, assignment, category, queueDef, queueTopic, role, group, company, timesheet, timesheetProfile, expense, dashboard, board, iteration, risk, billingRecord, documentVersion, predecessor, rsrcPool, staffingPlan, staffingPlanResource, nonLaborResource, rateAttribute, exchangeRate, booking, customEnum, parameterValue, uiTemplate, accessLevel, workspace, record_type, record_field, nlbrCategory, milestonePath, project_health, board_column, board_card, board_tag, dashboard_widget, userDelegation, time_phased_duration, time_phased_currency, template, templateTask, teamMember, schedule, expenseType, objectCategory, hourType, rateCard, approval, approval_stage, approval_stage_participant, documentApproval. |
| `insights_search_fields` | Search and find Workfront fields (columns) for projects, tasks, issues, users, portfolios, programs, teams — to filter on, sort by, or display in find_workfront_data(), ranked by relevance to the question. |
| `insights_summarize_object` | Fetch and summarize a single Workfront object — project, task, issue, user, portfolio, program, document — as a markdown narrative. |
| `workflow_create_any_object` | Create a new Workfront object of the given objectType. Supported types: projects, tasks, issues/requests, portfolios, programs, hour entries, assignments, plus users, teams, roles, and groups (admin only). The 'data' parameter is REQUIRED on every call and must include the object's 'name' plus any required parent IDs — never invoke this tool with an empty or partial payload. Task names cannot contain colon (:), forward slash (/), double quote ("), or curly braces ({ }) — Workfront rejects these characters; remove them before calling. When the user references a parent or related object by name (project, portfolio, owner, assignee), FIRST call insights_find_id_by_name (entity = the objectType) to obtain its ID, THEN call create_any_object in the same turn — do not stop after the lookup. If the result is ambiguous, ask the user to choose from the returned options instead of guessing. Use a field's `name` from insights_search_fields as the data key; if `name` is not set, fall back to its `label`. Discover unknown fields with insights_search_fields before adding them. You MUST call read_workflow_docs with workfront://tools/create-any-object BEFORE this tool whenever the request involves a status/enum value, a filter modifier other than plain equality (_Mod/_Range), a date comparison, a scheduling or date field on a project or task, a task dependency field, a relation path, result ordering, or a force delete. For some accounts, a slow create returns { status: "pending", jobId, message } instead of the created object — poll check_job_status until completed or failed. |
| `workflow_update_any_object` | Update an existing Workfront object by ID. Supported types: projects, tasks, issues/requests, portfolios, programs, hour entries, assignments, plus users, teams, roles, and groups (admin only). The 'data' parameter is REQUIRED and must contain at least one field to change. 'data' is a FLAT key/value map — custom fields belong directly at the top level with the DE: prefix. Task names cannot contain colon (:), forward slash (/), double quote ("), or curly braces ({ }). When the user identifies the target (or any related object) by name, FIRST call insights_find_id_by_name to obtain its ID, THEN call update_any_object in the same turn. Use a field's `name` from insights_search_fields as the data key. You MUST call read_workflow_docs with workfront://tools/update-any-object BEFORE this tool whenever the request involves a status/enum value, a filter modifier other than plain equality, a date comparison, a scheduling field, a task dependency field, a relation path, result ordering, or a force delete. |
| `workflow_delete_any_object` | Delete a Workfront object by ID. Supported types: projects, tasks, issues/requests, portfolios, programs, hour entries, assignments, plus users, teams, roles, and groups (admin only). MUST obtain explicit user confirmation (object type, ID, and name) before invoking, unless the user's current message already contains explicit confirmation language. When the user identifies the target by name, FIRST call insights_find_id_by_name to obtain its ID, THEN call delete_any_object in the same turn. 'force: true' bypasses the Recycle Bin and is permanent; warn the user and require a second confirmation before passing it. You MUST call read_workflow_docs with workfront://tools/delete-any-object BEFORE this tool whenever the request involves a status/enum value, a filter modifier other than plain equality, a date comparison, a scheduling field, a task dependency field, a relation path, result ordering, or a force delete. |
| `workflow_read_workflow_docs` | Fetch the full Workfront guide or reference doc for a workfront:// URI and return it as markdown. This server's detailed guidance lives behind this tool because the claude.ai connector does not auto-load MCP resources. Call this whenever a tool description or the server instructions point you to a workfront:// URI. Fetch the relevant guide BEFORE non-trivial query/report/filter, status, or deep-link work instead of guessing syntax, field names, or status codes. |
| `list_resources` | List all MCP resources and resource templates available on this lane. Returns prefixed URIs that can be passed to read_resource. Use this when your MCP client does not expose resources/list to the model. |
| `read_resource` | Read an MCP resource from any backend on this lane by its prefixed URI (the `<backend>+<scheme>://<rest>` form returned by list_resources). Use this when server init instructions tell you to load a resource but your MCP client does not expose resources/read to the model. Works for every URI scheme any configured backend publishes. |
| `approvals_create_approval_from_template` | Creates an approval workflow for one document version using an existing template. template_id must be the template GUID. Accepts one document version per call. |
| `approvals_create_or_update_approval_workflow` | Use this for any request to set up, add, or change approvers or reviewers on a document — e.g. 'create an approval with X as approver', 'add a review stage'. This is the dedicated tool for document approval workflows. Create or update the approval workflow stages for a document version. Every stage must declare its prerequisites via `dependsOn` (use [] for the first stage / parallel roots). Most workflows are LINEAR: stages[0].dependsOn=[], stages[1].dependsOn=[0], stages[2].dependsOn=[1], and so on. Provide all desired stages — this replaces or extends the existing workflow. WARNING: for a stage matched to an existing one, the `participants` array you provide REPLACES that stage's entire participant list. Supports per-stage plain-text customMessage shown in the first-activation notification email. |
| `approvals_create_template` | Create a new reusable approval workflow template. This is the dedicated tool for creating approval templates. Use it only when the user explicitly asks to create a reusable template; to set up an approval on a specific document, use create_or_update_approval_workflow. Every stage must declare its prerequisites via `dependsOn`. Resolve every participant to its ID before calling this: use Insights `search_users` for people when available, otherwise use `find_user_by_name`; use `find_team_by_name` for teams. Never pass a participant name. |
| `approvals_delete_approval_stage` | Delete a single stage from an approval workflow by name or position (e.g. 'Legal Review', 'stage 2', 'last stage'). Only not-started stages can be deleted. For approvals with parallel paths, use remove_stage_from_path instead — this tool only works on single-path approvals. |
| `approvals_find_document` | Look up a document by filename OR by document version ID (DOCV) — provide exactly one. Use document_name when the user names a file; use document_version_id when you have a DOCV from a Workfront URL or another tool. Returns document name, current version ID, and a Workfront document link per match (for name search) or full metadata (for version ID lookup). Matching by name is word-order-tolerant. |
| `approvals_find_projects` | Look up Workfront projects, optionally filtered by name and/or restricted to projects the calling user owns. Use a name fragment when the user names a project; set owned_by_me when the user says "my projects", "projects I own", or "what am I working on". Returns project name, ID, and status for each match — feed a project ID to get_documents_by_scope, then approval tools on the documents. |
| `approvals_find_team_by_name` | Look up a Workfront team's ID by name (fuzzy/partial match). Call this whenever the user wants a whole team — not a single person — as an approver or reviewer. Approval and template tools accept teams as participants with participantType: "TEAMOB" and the ID this tool returns. The word "team" is stripped from the query automatically. |
| `approvals_find_user_by_name` | Look up a Workfront user's ID by name (fuzzy/partial match). Call this whenever the user mentions a person by name — downstream tools like create_or_update_approval_workflow, create_template, and send_approval_reminder all require Workfront user IDs, not names. Returns name, ID, email, title, and avatar URL. |
| `approvals_get_approval_info` | Get the current approval workflow (stages, participants, status, stage IDs) for a document version. Each participant is listed with its participantId and, for AI Collaborator bots, its participantMetadata. REQUIRED before sending reminders or modifying an existing approval — returns the stage IDs that send_approval_reminder and delete_approval_stage need. |
| `approvals_get_current_user` | Get the calling user's Workfront identity: name, user ID, home team name, and home team ID. Use when the user refers to themselves ("me", "my projects", "approvals I own") and you need a concrete ID. Returns a single user every call — no disambiguation needed. Do not call this just to learn who the user is at the start of a conversation; only call it when an ID is actually required. |
| `approvals_get_documents_by_scope` | List documents (with name and current version ID) inside a project or folder. scope_type=project → returns all documents in that project. scope_type=folder → scope_id = folder ID directly. scope_type=folder_name → scope_id = project ID plus folder_name. Returns a numbered list of documents with name and document version ID (DOCV). |
| `approvals_list_templates` | List approval templates available in this Workfront instance. Use when the user wants to browse what's available or when picking one to instantiate with create_approval_from_template. Filters compose with AND: name, creator_name, participant_name, sort_by_usage. |
| `approvals_send_approval_reminder` | Send reminder email(s) to participants in approval stage(s). Works for a single approval or in bulk. SINGLE MODE requires document_version_id and stage_id from get_approval_info. BULK MODE provides an 'approvals' array; all approvals must come from ONE project/folder/program/campaign, max 100 per request. |
| `approvals_update_approval_participants` | SINGLE-PROJECT ONLY. STEP 1 of 2 (preview only — makes NO changes). Validates an add/remove/replace of a participant on the active stage of up to 100 document approval workflows within that single project and returns { requestId, preview }. Not a multi-project tool. If the user asks for a broader scope, STOP and ask for the ONE specific project. After confirmation, call _submit_bulk_update_approval_participants with the returned requestId. |
| `approvals_update_template` | Update an existing template with structured modifications (add/remove participants, rename stages, set deadlines, etc.). Batch all changes in a single call. To apply a modification to every stage at once, set stage_names: ["all"]. Obtain the template_id with list_templates using the name filter. |
| `approvals__submit_bulk_update_approval_participants` | STEP 2 of 2 (applies changes). Executes a bulk participant update previously previewed by update_approval_participants. Only call this after the user has explicitly confirmed the preview. Pass the requestId returned by the preview step; the request is consumed on use and cannot be replayed. |
| `boards_add_card_member` | Assign an existing board member to a card. member_id is the member's id as listed in the board members or a card's members array (see get_board_snapshot / get_card). |
| `boards_add_card_tag` | Add an existing board tag to a card. Tag ids come from the board tags list (see get_board_snapshot). |
| `boards_archive_card` | Archive or restore a card. This is the reversible alternative to deleting a card. |
| `boards_connect_card` | Connect a card to Workfront by creating a new Workfront Task or Issue under the given project and linking it to the card. The new Workfront item takes its name/description/estimation from the card. Resolve project_id and project_name with the Workfront insights tools first. Returns the connected card. |
| `boards_create_board` | Create a new basic board, optionally inside a workspace. Creates a standard board (no dynamic/kanban/connected-service setup — those are configured in the web UI). Returns the new board id, name, and template. |
| `boards_create_board_tag` | Create a new tag in a board's tag catalog (then use add_card_tag to apply it). Returns the new tag. Tag names must be unique on the board. |
| `boards_create_card` | Create a new card on a board in the given column. Returns the created card. To assign members or move the card afterwards, use add_card_member / move_card. |
| `boards_create_checklist_item` | Add a checklist item (or subtask) to a parent card. Returns the new item. Use update_checklist_item to rename or mark it done. |
| `boards_create_column` | Add a new plain column to a board. Creates a standard column with the given name (no WIP limits, automation, or intake/dynamic filters — those are configured in the web UI). Returns the board id and its columns so you can find the new column id. |
| `boards_delete_checklist_item` | Delete a checklist item from its parent card. Returns the updated parent card. |
| `boards_delete_column` | Delete a column from a board. If the column still has cards, pass to_column_id to move them to another column — otherwise the delete is rejected so cards are not lost. Returns the board id and its remaining columns. |
| `boards_disconnect_card` | Disconnect a card from its Workfront source. Pass the card's connectedObjectId (exposed on a connected card). Returns the card with its connection cleared. Does not delete the underlying Workfront item. |
| `boards_get_board_snapshot` | Fetch a board by id (cards + columns) via the in-process Nest BoardsService. |
| `boards_get_board_summary` | Normalized pipeline view for one board: columns, slim cards, aggregates, explicit gaps. |
| `boards_get_card` | Fetch a single card by board id and card id. |
| `boards_list_boards` | Discover boards from the authenticated user's workspaces. |
| `boards_list_candidates` | Return simplified candidate rows: id, name, description, columnId. |
| `boards_move_card` | Move a card to a different column and/or position. to_index is the 0-based slot within the destination column. |
| `boards_remove_card_member` | Remove a member from a card. member_id is the member's id as listed in the card's members array (see get_card). |
| `boards_remove_card_tag` | Remove a tag from a card. Tag ids come from the board tags list (see get_board_snapshot). |
| `boards_summarize_boards` | Summarize one or more boards. Use list_boards first to discover ids the authenticated user can access. |
| `boards_update_card` | Update mutable fields on an existing card (name, description, start/due date, status, estimation). At least one field must be provided. Use move_card to change column, archive_card to archive/restore, and the tag/member tools for tags and assignees. |
| `boards_update_checklist_item` | Update a checklist item (name, description, due date, status, estimation). At least one field must be provided. `status` is a status id from the board status catalog (not a label like "done"). |
| `boards_update_column` | Rename a board column. WIP limits, automation, and filters are configured in the web UI and are not editable here. Returns the updated column. |
| `comment-stream_add_reaction` | Add a reaction (like) to a comment. Currently only "LIKE" reactions are supported. Returns the reaction details including commentID, userID, and type. RECORD objects (objectCode: "RECORD"): the response will NOT include a `links` field — construct the URL client-side using the maestro record URL pattern. |
| `comment-stream_create_comment` | Create a new comment on an object. Supports rich text, HTML content, tags, and attachments. When tagging users or teams, use the "tags" array with objects shaped as { id, objCode: "USER"|"TEAMOB" }; for users, ALWAYS use the Workfront wfId (32-char hex). Use dry_run: true first to preview and confirm before posting — a dry run never sends notifications. RECORD objects require objectMetadata (name, url) when tagging users. |
| `comment-stream_create_reply` | Create a reply to an existing comment. Supports rich text, HTML content, tags, and attachments. Same mention/tag rules as create_comment. Use dry_run: true first to preview and confirm before posting. |
| `comment-stream_delete_comment` | Delete a comment by its ID. Marks the comment as deleted and updates the deleted metadata. Users can only delete their own comments or must have admin privileges. |
| `comment-stream_get_comment` | Get a single comment by its ID. Returns detailed information about a specific comment. |
| `comment-stream_get_comments_count` | Get the total count of top-level comments for a specific object, matching what `query_comments` would return across all pages. For container objects (e.g. PROJECT), the count includes comments on child objects the same way the listing tool does. Replies are not included in the count. |
| `comment-stream_query_comments` | Query comments by object ID with pagination support. Returns a list of comments for a specific object (e.g., BOARD, TASK, etc.) with cursor-based pagination. |
| `comment-stream_remove_reaction` | Remove a reaction (like) from a comment. Currently only "LIKE" reactions are supported. Returns acknowledgment of the removal. |
| `comment-stream_update_comment` | Update an existing comment or reply. Can update content, tags, attachments, and metadata. Note: Comments can only be edited within 15 minutes of creation. When tagging users or teams, use the "tags" array with the Workfront wfId for users. |
| `planning_add_global_record_type_to_workspace` | Add a global record type to a workspace. WARNING: The workspace MUST have a section first—call update_workspace to add one if needed. |
| `planning_bulk_record_actions` | Create, update, delete, or restore records in a single request. Use for all record mutations. Before writing, read schema://record-type/{recordTypeId} to learn field IDs, value formats, and writable fields. To LINK an existing external object to a record, do it here as an update: set the record's connection/reference field value to the external object's raw Workfront ID. When the user does not provide a record ID for an update or delete, call search_records first. Not atomic — check the hasErrors flag. When tool B needs an ID returned by tool A, call them in SEPARATE turns. |
| `planning_convert_workspace_to_template` | Convert an existing workspace into a reusable template. Requires admin privileges. |
| `planning_create_connection_record` | Create a brand-new record/object in an external connected system (e.g. create a new Workfront project, portfolio, or program). The record type must be an external record type linked to a supported connection object. Supported Workfront object types: PROJECT, PORTFOLIO, PROGRAM. IMPORTANT: Use this ONLY to create a brand-new external object. To LINK an EXISTING Workfront project to a Planning record, use bulk_record_actions instead. |
| `planning_create_fields` | Create one or more fields (columns) on a record type. PREREQUISITE: Call get_record_type first to check existing field names (displayName must be unique). Read docs://field-types for available types and options. For formula fields, also read docs://formula-syntax. LOOKUP / ROLLUP FIELDS: Use type "lookup" with lookupOptions containing referenceFieldId, sourceFieldId, and optionally rollup. Not atomic — check the hasErrors flag. |
| `planning_create_record_types` | Create one or more record types. A default table view is created automatically for each. PREREQUISITE: The workspace must have a section — call update_workspace to add one if needed. Read docs://workspace-setup-guide for best practices. isGlobal=true requires admin privileges; default to local record types. Not atomic — check the hasErrors flag. |
| `planning_create_view` | Create a new view for a record type. Views define how records are displayed: table (spreadsheet), timeline (Gantt chart), or calendar. Can configure filters, sorting, and grouping during creation. Read docs://view-types for view type requirements and capabilities. For filter configuration, read docs://filter-operators. |
| `planning_create_workspace` | Create a new empty workspace. A workspace is a container for organizing related record types, fields, and data. Read docs://workspace-setup-guide for best practices. Hierarchy: Workspace → Section → RecordType → Field/View/Record. |
| `planning_create_workspace_from_template` | Create a new workspace from a template. The template provides pre-configured record types, fields, views, and sample data. Use get_template_list to find available templates and get_template for full template details. |
| `planning_delete_field` | Delete a Field by ID. This permanently removes the field from its record type, including all data stored in this field across all records. This action cannot be undone. The field must exist and the user must have EDIT permission on the record type. |
| `planning_delete_record_type` | Delete a Record Type by ID. This will permanently remove the record type and ALL its data including all records, fields, and views. Requires MANAGE permission. WARNING: This action cannot be undone. |
| `planning_delete_view` | Delete a view by ID. This permanently removes the view configuration but does NOT delete records. Requires MANAGE permission. Cannot be undone. The last table view in a record type cannot be deleted. |
| `planning_delete_workspace` | Delete a workspace by ID. Permanently removes the workspace and all its data. Requires MANAGE permission. Use workspace ID only (not name/alias); use get_workspace_list to find the ID if needed. |
| `planning_get_external_record_workspaces` | Find which Maestro workspaces and record types are connected to a specific external object. Use this to check whether an external record (e.g. a Workfront project) is already linked to a Maestro record, or to discover which workspaces/record types can accept a connection for that object type. See docs://connections for available connections and their supported object types. |
| `planning_get_field` | Get Field details by ID. Returns field configuration and a fieldValueSchema describing how to format values for this field when creating or updating records. TIP: To see all fields on a record type at once, prefer get_record_type with attributes=['id','displayName','fields']. |
| `planning_get_record` | Get Record details by ID. Returns all attributes. |
| `planning_get_record_change_log` | Get field change history for a record. Returns chronological list of field value changes (most recent first) showing what changed, when, and by whom. Includes before/after values. Useful for audit trails. Excludes system field changes. Supports pagination via offset/limit. |
| `planning_get_record_sharing` | Get record sharing. Retrieves sharing/permissions information for a specific record, including who has access and their permission levels. Returns a wildcard access level (EVERYONE_IN_WORKSPACE or SELECTED_USERS_ONLY) and a list of sharing entries. Requires MANAGE permission on the record type containing the record. |
| `planning_get_record_type` | Get Record Type details by ID. Retrieves complete information about a specific record type including its configuration, fields, permissions, metadata, and a summary of all views (table/timeline/calendar). For global record types, also includes global settings. For per-field value schemas, read the schema://record-type/{recordTypeId} resource. |
| `planning_get_record_type_sharing` | Get record type sharing. Retrieves sharing/permissions information for a record type, including who has access and their permission levels. Requires MANAGE permission on the workspace containing the record type. For dynamic (global) record types, use scopeWorkspaceId. |
| `planning_get_template` | Get template details by template ID, projected at the requested detail level. Use 'minimal' for just identity and counts, 'metadata' (default) for the embedded workspace with record-type metadata but no fields, or 'full' for the complete legacy payload. |
| `planning_get_template_list` | List all available templates with lightweight summary info (ID, name, description, type, icon). Use get_template with the template ID to retrieve full details. |
| `planning_get_view` | Get complete view details by ID. Returns view configuration including name, type (table/timeline/calendar), metadata (date fields, column settings), filters, sorting, grouping, permission level, sharing status, and audit fields. |
| `planning_get_view_sharing` | Get view sharing. Retrieves sharing/permissions information for a view, including who has access and the view's public sharing status. Requires MANAGE permission on the view. |
| `planning_get_workspace` | Get workspace details by ID or alias. Returns workspace metadata, sections, and a list of record types projected at the requested detail level (defaults to 'metadata'). Use 'minimal' for the smallest payload or 'full' to inline every field (can produce multi-MB responses). |
| `planning_get_workspace_list` | Get workspaces with cursor-based pagination. Returns a lightweight summary for each workspace (id, name, description, permission, createdBy/At, updatedBy/At). Use get_workspace with a specific workspace ID for full details. |
| `planning_get_workspace_sharing` | Get workspace sharing. Retrieves sharing/permissions information for a workspace, including who has access and their permission levels. Requires MANAGE permission on the workspace. |
| `planning_list_addable_global_record_types` | List global record types available to add to a specific workspace. Returns centrally-defined record types that the current user is authorized to add to the specified workspace, excluding those already linked. |
| `planning_list_global_record_types` | List all global record types in the organization. Returns the full catalog of centrally-defined record types visible to the current user, regardless of workspace. Use list_addable_global_record_types to see what can be added to a specific workspace. |
| `planning_modify_record_type_sharing` | Modify record type sharing. Add or remove user/group/team/role/company permissions. Permission levels: administrator, member, viewer, inherit_view_only. License type may restrict levels. Auto-adds workspace viewer access for new users. Cannot remove owner. Verify user existence with search_users before calling this tool. |
| `planning_modify_records_sharing` | Modify record-level sharing. Requires MANAGE permission on the record type. Auto-adds workspace viewer for new users. To GRANT access: use relationsToCreate with relation viewer or member. To REVOKE access: use relationsToCreate with relation no_access. Set wildcard to EVERYONE_IN_WORKSPACE to control default access. SELECTED_USERS_ONLY is not supported. Verify user existence with search_users before calling this tool. |
| `planning_modify_view_sharing` | Modify view sharing. Views support only 2 levels: administrator (manage) and viewer (view). Can enable/disable public sharing via isPublicShared (requires workspace MANAGE). Cannot remove owner. Verify user existence with search_users before calling this tool. |
| `planning_modify_workspace_sharing` | Modify workspace sharing. Add or remove user/group/team/role/company permissions. Permission levels: administrator, member, viewer. License type may restrict available levels. Cannot remove owner or downgrade system admins. Verify user existence with search_users before calling this tool. |
| `planning_remove_global_record_type_from_ws` | Remove a global record type from a workspace. This removes only the instance from the specified workspace, not the source global record type. WARNING: All records of this type in the workspace will be permanently deleted. |
| `planning_search_records` | Search and filter records. IMPORTANT: text equality uses $is (not $eq). Read docs://filter-operators for all operators and filter syntax by field type. |
| `planning_search_resources` | Search across all resource types (workspaces, record types, views) by name. Returns results ranked by relevance. Each result includes the resource type and contextual details (e.g., workspace name for record types, view type for views). Use this tool to quickly find any object by name when you don't know its type or location. |
| `planning_search_sharing_data` | Search for users, groups, teams, job roles, and companies for sharing and permissions. IMPORTANT: This tool searches ONLY by name. Do NOT pass IDs, emails, or any other field values — only names are supported. General-purpose API for finding entities to share workspaces, record types, records, or views with. |
| `planning_search_users` | Search for users with pagination. Supports two modes (use exactly one): (1) By IDs — provide 'ids' to look up specific users by their IDs. (2) By search term — provide 'searchTerm' to match against name, email, user ID, or IMS user ID. NOTE: the user 'id' field in results is the IMS user ID, not the Workfront User ID; the Workfront User ID is in 'wfId'. Returns an empty list when no users match. |
| `planning_update_field` | Partially update a field by ID. Only provided fields change; others are preserved. A field's type cannot be changed after creation. For lookup fields: rollup can be changed via lookupOptions.rollup. Read docs://field-types for type-specific options that can be updated. |
| `planning_update_record_type` | Partially update a record type. Only provided fields change; omitted fields retain their current values. |
| `planning_update_records_order` | Update records order within a record type. Moves specified records to a new position relative to another record. You must provide either putUnderRecordId (place after) or putBeforeRecordId (place before), and at least one record ID to move. |
| `planning_update_view` | Partially update an existing view. Only the fields you provide will change; omitted fields remain untouched. Read docs://view-types for view type configuration details. For filter updates, read docs://filter-operators. |
| `planning_update_workspace` | Update an existing workspace. Partial update: only provided fields change, null fields are ignored. When tool B needs an ID returned by tool A, call them in SEPARATE turns. Hierarchy: Workspace → Section → RecordType → Field/View/Record. |

### Prompts

_No prompts listed._

---

## Installation Steps

Check https://experienceleague.adobe.com/en/docs/workfront/using/basics/workfront-mcp-server/configure-workfront-mcp-server#connect-workfront-to-a-custom-mcp-solution for installation instructions.

---

## Accessing the MCP Server

OAuth 2.0 DCR with user IMS and OAuth 2.0 with ClientID and ClientSecret

---

## Documentation

- https://experienceleague.adobe.com/en/docs/workfront/using/basics/workfront-mcp-server/workfront-mcp-server
- https://experienceleague.adobe.com/en/docs/workfront/using/basics/workfront-mcp-server/workfront-mcp-server-overview
- https://experienceleague.adobe.com/en/docs/workfront/using/basics/workfront-mcp-server/configure-workfront-mcp-server
- https://experienceleague.adobe.com/en/docs/workfront/using/basics/workfront-mcp-server/use-workfront-mcp-server
- https://experienceleague.adobe.com/en/docs/workfront/using/basics/workfront-mcp-server/workfront-mcp-server-tools

---

## Support

aidevusworkfront@adobe.com
